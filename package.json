{
  "name": "ambev-jobinterview",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cypress install",
    "cypress:install": "cypress install",
    "cypress:open": "cypress open",
    "cypress:open:safe": "cross-env ELECTRON_EXTRA_LAUNCH_ARGS=--disable-gpu cypress open",
    "cypress:run": "cypress run",
    "cypress:pipeRun": "cross-env CI=true GITHUB_ACTIONS=true cypress run"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "cypress-plugin-api": "^2.11.2",
    "cross-env": "^7.0.3",
    "cypress": "^13.17.0"
  }
}
